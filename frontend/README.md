# Frontend (React + Vite)

This frontend is intentionally minimal and shows how to:

- use routed pages (`src/components/pages`)
- centralize API calls (`src/services/tournamentService.js`)
- share state via context (`src/context/TournamentContext.jsx`)

Replace these examples with your product UI while keeping the same boundaries.
