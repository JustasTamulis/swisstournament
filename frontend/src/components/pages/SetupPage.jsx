const SetupPage = () => (
  <section>
    <h2>Setup Guide</h2>
    <ol>
      <li>Clone this repository and run <code>make bootstrap</code>.</li>
      <li>Run <code>make dev</code> to start backend + frontend in separate terminals.</li>
      <li>Replace template models/endpoints with your product domain.</li>
      <li>Follow README deployment steps to ship to Heroku.</li>
    </ol>
  </section>
);

export default SetupPage;
