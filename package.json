{
  "name": "django-react-webapp-template",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend run build -- --emptyOutDir"
  },
  "engines": {
    "node": ">=20"
  },
  "cacheDirectories": [
    "frontend/node_modules"
  ]
}
