{
  "name": "webapp-template",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install --include=dev && npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "cacheDirectories": [
    "frontend/node_modules"
  ]
}
